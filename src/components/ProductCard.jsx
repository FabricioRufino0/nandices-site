import React from 'react';
import imageMetadata from '../data/imageMetadata.json';
import WA from './WhatsApp.jsx';
export function Photo({product,className='',hero=false}) {const dimensions=imageMetadata[product.image]||{width:960,height:960};return <img className={className} src={`${product.image}-480.webp`} srcSet={`${product.image}-320.webp 320w, ${product.image}-480.webp 480w, ${product.image}-960.webp 960w`} sizes={hero?'100vw':'(max-width:520px) calc(100vw - 44px), (max-width:900px) 45vw, 380px'} width={dimensions.width} height={dimensions.height} alt={`${product.name} da Nandices Confeitaria`} loading={hero?'eager':'lazy'} fetchPriority={hero?'high':'auto'}/>}
export function ProductDescription({text}) {return <p className="product-description">{text}</p>}
export default function ProductCard({product}) {const message=`Olá! Vim pelo site da Nandices e gostaria de consultar o docinho ${product.name}.`;return <article><div className="product-image"><Photo product={product}/></div><div className="product-meta"><span>{product.category}</span><span>≈ 16 g</span></div><h3>{product.name}</h3><ProductDescription text={product.description}/><WA className="text-link" event="whatsapp_product" cta_location="catalogo" product_name={product.name} product_category={product.category} message={message}>Consultar pelo WhatsApp</WA></article>}
